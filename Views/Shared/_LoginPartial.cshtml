
@using Microsoft.AspNetCore.Identity
@using ProjectCarRental.Models
@inject             SignInManager<myAppUsers> SignInManager
@inject             UserManager<myAppUsers> UserManager
<style>
    
    .custom_a:hover{
        color:#0000!important;
    }
</style>
<ul class="navbar-nav" style="list-style: none; padding: 0; display: flex; align-items: center;">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item me-5">
            <a style="height: 40px; width: 190px; margin-right:1.5rem" class=" btn btn-outline-light rounded-pill nav-link  text-light custom_a" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" style="height: 40px; width: 90px; margin-right:3rem" class="me-1 btn btn-outline-light rounded-pill nav-link btn btn-link text-light custom_a">Logout</button>
        </form>
    </li>
}
else
{
        @* <li class="nav-item" style="margin-left: 3rem;">
            <a style="height: 40px; width: 90px; display: flex; align-items: center; justify-content: center; text-decoration: none; color: white; border: 1px solid white; border-radius: 50px;" class="nav-link" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item" style="margin-right: 3rem;">
            <a style="height: 40px; width: 90px; display: flex; align-items: center; justify-content: center; text-decoration: none; color: white; border: 1px solid white; border-radius: 50px;" class="nav-link" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li> *@
    <li class="nav-item ms-5">
            <a style="height: 40px; width: 90px; margin-right:1.5rem" class="btn btn-outline-light rounded-pill ms-3 nav-link text-light" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item me-5">
            <a style="height: 40px; width: 90px; margin-right:3rem" class="btn btn-outline-light rounded-pill me-4 nav-link text-light" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>
